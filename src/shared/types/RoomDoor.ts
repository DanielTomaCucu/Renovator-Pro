/** Configurarea unei uși a camerei — se scade din plinta/faianța/vopseaua/tapetul peretelui pe care e amplasată (max. o ușă per perete). */
export interface RoomDoor {
  width: number;
  height: number;
}
